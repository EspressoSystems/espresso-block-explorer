import { default as React } from 'react';
/**
 * SortIndicator is a component that displays the sort indicator for the
 * table header based on the current sort state.
 */
export declare const SortIndicator: React.FC;
