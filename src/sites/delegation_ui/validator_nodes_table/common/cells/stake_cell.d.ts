import { default as React } from 'react';
/**
 * StakeCell displays the stake amount for a validator node, along with its
 * percentage of the total stake.
 */
export declare const StakeCell: React.FC;
