import { default as React } from 'react';
/**
 * ManageStakeContent is a React component that displays the content
 * for managing stake in the staking modal.
 */
export declare const ManageStakeContent: React.FC;
