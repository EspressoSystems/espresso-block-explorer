import { default as React } from 'react';
/**
 * EpochEndsIn displays the current epoch number and the estimated time
 * remaining until the end of the current epoch.
 */
export declare const EpochEndsIn: React.FC;
