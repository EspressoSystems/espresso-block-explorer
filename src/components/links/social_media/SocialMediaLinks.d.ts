import { default as React } from 'react';

interface SocialMediaLinksProps extends React.DetailedHTMLProps<React.AnchorHTMLAttributes<HTMLDivElement>, HTMLDivElement> {
}
/**
 * SocialMediaLinks is a component that contains the quick icon link references
 * to all of the Espresso Social Media Links grouped together.
 */
declare const SocialMediaLinks: React.FC<SocialMediaLinksProps>;
export default SocialMediaLinks;
