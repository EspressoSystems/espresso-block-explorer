import { default as React } from 'react';
/**
 * HelpQuestion1 represents an icon that is a circle with a "?" symbol in the
 * center.
 */
declare const HelpQuestion1: React.FC<React.SVGProps<SVGElement>>;
export default HelpQuestion1;
