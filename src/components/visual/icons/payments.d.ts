import { default as React } from 'react';
/**
 * Payments is an icon used to represent payments or transactions.
 */
declare const Payments: React.FC<React.SVGProps<SVGElement>>;
export default Payments;
