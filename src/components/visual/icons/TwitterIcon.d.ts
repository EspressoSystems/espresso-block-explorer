import { default as React } from 'react';

/**
 * TwitterIcon is an icon representation of the Twitter logo.
 */
declare const TwitterIcon: React.FC<React.SVGProps<SVGElement>>;
export default TwitterIcon;
