name: Docker Build

on:
  push:
    branches:
      - main
      - release-*
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Install QEMU
        uses: docker/setup-qemu-action@v3
        with:
          cache-image: false
          platforms: arm64,amd64

      - name: Setup Docker BuildKit
        uses: docker/setup-buildx-action@v3

      - name: Login to Github Container Repo
        uses: docker/login-action@v3
        if: github.event_name != 'pull_request'
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Block Explorer - Generate Docker Metadata
        uses: docker/metadata-action@v5
        id: be_meta
        with:
          images: ghcr.io/EspressoSystems/espresso-block-explorer

      - name: Delegation UI - Generate Docker Metadata
        uses: docker/metadata-action@v5
        id: du_meta
        with:
          images: ghcr.io/EspressoSystems/espresso-delegation-ui

      - name: Block Explorer - Build and Push Docker
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/block-explorer.Dockerfile
          platforms: linux/amd64,linux/arm64 #arm64 timing out currently
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.be_meta.outputs.tags }}
          labels: ${{ steps.be_meta.outputs.labels }}

      - name: Delegation UI - Build and Push Docker
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/delegation-ui.Dockerfile
          platforms: linux/amd64,linux/arm64 #arm64 timing out currently
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.du_meta.outputs.tags }}
          labels: ${{ steps.du_meta.outputs.labels }}
