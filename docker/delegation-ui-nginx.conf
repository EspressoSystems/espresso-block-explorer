server {
    listen 3000;
    listen [::]:3000;

    root /usr/share/nginx/html;
    index index.html;

    # Static JS/CSS assets are content-hashed; cache them indefinitely.
    location /_next/static/ {
        expires max;
        add_header Cache-Control "public, immutable";
    }

    # config.json is regenerated at each container start; Only have the client
    # cache it for up to 10 minutes.
    location = /config.json {
        add_header Cache-Control "max-age=600, s-maxage=600, public";
    }

    # All other routes serve index.html (client-side routing).
    location / {
        try_files $uri $uri.html $uri/ /index.html;
    }
}
